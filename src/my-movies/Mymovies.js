import { LitElement, html } from "lit-element";
import { ScopedElementsMixin } from "@open-wc/scoped-elements";

export class PopcornMyMovies extends ScopedElementsMixin(LitElement) {
  render() {
    return html` <h2>Hello</h2> `;
  }
}
